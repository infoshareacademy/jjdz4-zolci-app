package pl.isa.autoparts.questions;


import com.fasterxml.jackson.databind.ObjectMapper;
import org.apache.commons.lang3.StringUtils;


import java.nio.charset.StandardCharsets;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.Scanner;


public class QuestionParserXML {

    public static void main(String[] args) {

//        ObjectMapper objectMapper = new ObjectMapper();
//        TopClass topClass = objectMapper.readValue(
//
//                StringUtils.toEncodedString(Files.readAllBytes(
//                        Paths.get("src/main/resorces/questions.xml"))
//                        , StandardCharsets.UTF_8), TopClass.class);
//
//        Scanner sc = new Scanner("src/main/resorces/questions.xml")

    }
}

//    Employees employees = objectMapper.readValue(
//
//            StringUtils.toEncodedString(Files.readAllBytes
//                    (Paths.get("/tmp/employees.xml")),
// StandardCharsets.UTF_8),
//
//            Employees.class);